export function handleCamma(arr) {
  //  Set comma for readibility.

  let isDot = false;
  for (let i = 0; i < arr.length; i++) {
    // if (val === ".") isDot = true;
    // if (!isDot && index % 3 === 0) {
    //   return;
    // } else !isDot && index === 1 && arr.length;
  }
}
